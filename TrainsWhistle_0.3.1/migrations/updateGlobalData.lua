for key,value in pairs(global.listTrains) do
	global.listTrains[key].guiData=global.listTrains[key].guiData or value
end

for key,value in pairs(global.listTrainStops) do
	global.listTrainStops[key].guiData=global.listTrainStops[key].guiData or value
end